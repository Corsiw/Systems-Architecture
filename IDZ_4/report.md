# 🏴‍☠️ Отчёт по ИЗД_4

## 1. Информация
**Тема:** Многопоточное приложение на POSIX API (задача «Остров Сокровищ»)  
**Вариант:** 32  
**Исполнитель:** *Терехов Дмитрий Сергеевич*  
**Группа:** *БПИ249*  
**Ожидаемая оценка** `10 баллов`

---

## 2. Условие задачи

Шайка пиратов под предводительством Джона Сильвера высадилась на берег Острова Сокровищ. Пиратам необходимо найти сокровище на острове, разделенном на участки. Каждой группе назначается участок, после чего группа ищет клад и возвращается к Сильверу с докладом. Поскольку число участков превышает число групп, каждая группа может обойти несколько участков. Сильвер сам не ходит по джунглям, а управляет поиском с берега.

Необходимо реализовать многопоточное приложение, моделирующее действия Сильвера и пиратов. Потоки должны представлять каждую группу и управляющий поток Сильвера.

---

## 3. Запуск программы
1. Компиляция

```bash
g++ -o treasure src/treasure.cpp
```

2. Запуск
```bash
./treasure -g 2 -s 10 -o files/a.txt
```

3. Компиляция версии на 9-10 баллов
```bash
g++ -o alter src/alter_primitives.cpp
```

4. Запуск
```bash
./alter -g 2 -s 10 -o files/b.txt
```

---

## 4. Сценарий решаемой задачи

1. **Субъекты задачи**:

   * Джон Сильвер — управляющий поток, принимает доклады от групп.
   * Поисковые группы пиратов — потоки, которые ходят по участкам острова, ищут клад и возвращают информацию Сильверу.

2. **Поведение субъектов**:

   * Каждая группа получает доступ к следующему свободному участку, ищет клад, формирует доклад и уведомляет Сильвера.
   * Сильвер ожидает доклады всех групп и обрабатывает их по мере поступления.

3. **Параллельная модель**:

   * Модель `Портфель задач` - Группы берут задачи из общего пула, Сильвер обрабатывает их результаты
   * Каждая группа работает как отдельный поток.
   * Сильвер также является отдельным потоком.
   * Синхронизация осуществляется с помощью:

     * **Мьютексов** для защиты массивов данных и консольного/файлового вывода.
     * **Семафоров** или **условной переменной** для уведомления Сильвера о поступивших докладах.
   * При решении на 9-10 баллов:
     * **Условных переменных** (`cond_report`) для быстрого и неблокирующегося уведомления Сильвера о новом отчете,
     * **Атомарных переменных** (`next_section`, `reports_count`) для безопасного доступа к индексам участков и докладов без блокировок.

---

## 5. Входные данные программы

Программа позволяет задавать параметры через:

1. **Командную строку**:

   * `-g` / `--groups` — число поисковых групп (целое > 0 и < числа участков)
   * `-s` / `--sections` — число участков (целое > 0)
   * `-i` / `--input-file` — альтернативный ввод из файла конфигурации
   * `-o` / `--output-file` — имя файла для вывода результатов

2. **Файл конфигурации**:

   ```
   <число групп> <число участков> [имя файла для вывода]
   ```

3. **Диапазоны случайных чисел**:

   * Время поиска: 200–1500 мс (`MIN_GROUP_DELAY`–`MAX_GROUP_DELAY`)
   * Вероятность нахождения сокровища: 0.05 (`TREASURE_PROB`)

> При многократных запусках результаты поиска различаются из-за генерации случайных чисел.

---

## 6. Реализация программы

### 6.1 Основная версия (семафор + мьютексы)

* Потоки групп используют семафор для уведомления Сильвера о готовых докладах.
* Мьютексы защищают доступ к массиву докладов и консоль/файл.
* Главный поток запускает группы и управляет потоком Сильвера.

### 6.2 Альтернативная версия (atomic + pthread_cond_t)

* **Атомарные переменные**:

  * `next_section` — следующий свободный участок.
  * `reports_count` — количество созданных докладов.
* **Условная переменная** `cond_report`:

  * Потоки групп уведомляют Сильвера о готовом докладе.
  * Сильвер ожидает доклад с помощью `pthread_cond_wait`.
* **Мьютексы** остаются для защиты массивов данных и вывода.
* Поведение идентично основной версии, но уменьшено количество блокировок. Это можно увидеть по выходным файлам [alter.txt](files/alter.txt) и [main.txt](files/main.txt), которые в целом ничем не отличаются, кроме изменений связанных с генерацией случайных чисел.

---

## 7. Завершение программы и обработка сигналов

* Программа корректно завершает работу после обработки всех участков.
* Поддерживается корректное завершение при нажатии Ctrl+C (`SIGINT`) или сигнале `SIGTERM`.
* Потоки проверяют глобальный флаг `g_terminate` для выхода из циклов.

---

## 8. Результаты работы программы

### 8.1 Примеры входных файлов

**`input_file_1.txt`**

```
2 10 files/output_file_1.txt
```

**`input_file_2.txt`**

```
10 2 files/output_file_2.txt
```

**`input_file_3.txt`**

```
5 50 files/output_file_3.txt
```

### 8.2 Примеры выходных файлов

**`output_file_1.txt`**

```
[Группа 1] Вышла на участок 0
[Группа 2] Вышла на участок 1
[Группа 2] Вышла на участок 2
...
[Сильвер] Работа завершена. Найдено кладов: 1
```

**`output_file_2.txt`**

Файл пуст, т.к. в файле находятся некорректные параметры - число участком меньше числа групп.

**`output_file_3.txt`**

```
[Группа 1] Вышла на участок 0
[Группа 3] Вышла на участок 1
...
[Сильвер] Работа завершена. Найдено кладов: 1
```

> Вывод на экран идентичен содержимому файлов. За исключением 2 случая. Через cerr в консоль выводится ошибка.

---

## 9. Сравнительный анализ

| Параметр              | Основная версия (semaphore + mutex) | Альтернативная версия (atomic + cond)                  |
| --------------------- | ----------------------------------- | ------------------------------------------------------ |
| Синхронизация         | Семафор для уведомления Сильвера    | Условная переменная `cond_report`                      |
| Защита индексов       | Мьютексы                            | Атомарные переменные                                   |
| Количество блокировок | Больше                              | Меньше, эффективность выше                             |
| Поведение программы   | Последовательное получение докладов | Идентичное, доклады обрабатываются по мере поступления |


**Вывод:**
Альтернативная версия полностью идентична по поведению основной версии при одинаковых входных данных, но обеспечивает более эффективную синхронизацию и уменьшает блокировки.

---

## 10. Заключение

* Все требования задания выполнены.
* Реализованы обе версии программы: с семафором и с условной переменной + атомарными переменными.
* Программа поддерживает ввод параметров из командной строки и из файла, вывод в файл и на экран.
* Предусмотрено корректное завершение по сигналам.
* Результаты работы программы представлены в виде файлов и консольного вывода.

---